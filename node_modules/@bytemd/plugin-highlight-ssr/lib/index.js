// @ts-ignore
import rehypeHighlight from 'rehype-highlight';
export default function highlightSsr({ subset = false, ignoreMissing = true, ...rest } = {}) {
    return {
        rehype: (u) => u.use(rehypeHighlight, { subset, ignoreMissing, ...rest }),
    };
}
