import {
  require_unist_util_visit
} from "./chunk-EQ5MQS3M.js";
import "./chunk-3RIRJ6BY.js";
import {
  __commonJS,
  __toModule
} from "./chunk-XTN4ZABK.js";

// node_modules/remark-breaks/index.js
var require_remark_breaks = __commonJS({
  "node_modules/remark-breaks/index.js"(exports, module) {
    "use strict";
    module.exports = breaks2;
    var visit = require_unist_util_visit();
    var find = /[\t ]*(?:\r?\n|\r)/g;
    var splice = [].splice;
    function breaks2() {
      return transform;
    }
    function transform(tree) {
      visit(tree, "text", ontext);
    }
    function ontext(node, index, parent) {
      var result = [];
      var start = 0;
      var match;
      var position;
      find.lastIndex = 0;
      match = find.exec(node.value);
      while (match) {
        position = match.index;
        if (start !== position) {
          result.push({ type: "text", value: node.value.slice(start, position) });
        }
        result.push({ type: "break" });
        start = position + match[0].length;
        match = find.exec(node.value);
      }
      if (result.length > 0) {
        if (start < node.value.length) {
          result.push({ type: "text", value: node.value.slice(start) });
        }
        splice.apply(parent.children, [index, 1].concat(result));
        return index + result.length;
      }
    }
  }
});

// node_modules/@bytemd/plugin-breaks/dist/index.esm.js
var import_remark_breaks = __toModule(require_remark_breaks());
function breaks() {
  return {
    remark: (u) => u.use(import_remark_breaks.default)
  };
}

// dep:@bytemd_plugin-breaks
var bytemd_plugin_breaks_default = breaks;
export {
  bytemd_plugin_breaks_default as default
};
//# sourceMappingURL=@bytemd_plugin-breaks.js.map
