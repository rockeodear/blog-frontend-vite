import "./chunk-XTN4ZABK.js";

// node_modules/@bytemd/plugin-highlight/dist/index.esm.js
function highlight({ init } = {}) {
  let hljs;
  return {
    viewerEffect({ markdownBody }) {
      (async () => {
        const els = markdownBody.querySelectorAll("pre>code");
        if (els.length === 0)
          return;
        if (!hljs) {
          hljs = await import("./lib-KO22GRLS.js");
          if (init)
            await init(hljs);
        }
        els.forEach((el) => {
          hljs.highlightElement(el);
        });
      })();
    }
  };
}

// dep:@bytemd_plugin-highlight
var bytemd_plugin_highlight_default = highlight;
export {
  bytemd_plugin_highlight_default as default
};
//# sourceMappingURL=@bytemd_plugin-highlight.js.map
